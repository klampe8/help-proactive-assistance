# Adobe Artifactory registry for @identity scope
@identity:registry=https://artifactory.corp.adobe.com/artifactory/api/npm/npm-adobe-release/
# Allow optional dependencies to fail without breaking the build
optional=true
